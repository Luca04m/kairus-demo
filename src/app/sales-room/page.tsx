import { SalesRoomPanel } from '@/components/sales-room/SalesRoomPanel';

export const metadata = {
  title: 'Sales Room | Kairus OS',
  description: 'Painel de monitoramento em tempo real da operacao de vendas e suporte.',
};

export default function SalesRoomPage() {
  return <SalesRoomPanel />;
}
