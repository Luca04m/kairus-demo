'use client';

// Re-export from the provider for backward compatibility.
// Components should prefer importing from '@/providers/SupabaseProvider' directly.
export { useSupabase } from '@/providers/SupabaseProvider';
